# Browser-facing API base URL. Include the full versioned /api/v1 path.
VITE_API_BASE_URL=/api/v1

# Local Vite proxy upstream. Use an origin only, without /api/v1.
VITE_API_PROXY_TARGET=http://localhost:3000
